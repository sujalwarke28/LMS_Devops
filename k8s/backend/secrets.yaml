# ─────────────────────────────────────────────────────────
# IMPORTANT: Create this secret with:
#   kubectl create secret generic lms-backend-secrets \
#     --from-literal=MONGODB_URI='<uri>' \
#     --from-literal=FIREBASE_PROJECT_ID='<id>' \
#     --from-literal=FIREBASE_PRIVATE_KEY='<key>' \
#     --from-literal=FIREBASE_CLIENT_EMAIL='<email>' \
#     --from-literal=FIREBASE_PRIVATE_KEY_ID='<kid>' \
#     --from-literal=FIREBASE_CLIENT_ID='<cid>' \
#     --from-literal=AWS_ACCESS_KEY_ID='<key>' \
#     --from-literal=AWS_SECRET_ACCESS_KEY='<secret>' \
#     --from-literal=AWS_S3_BUCKET_NAME='<bucket>' \
#     -n lms
# ─────────────────────────────────────────────────────────
# DO NOT store actual secret values here.
# This file is a reference template only.
apiVersion: v1
kind: Secret
metadata:
  name: lms-backend-secrets
  namespace: lms
  labels:
    app: lms-backend
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_ME"
  FIREBASE_PROJECT_ID: "REPLACE_ME"
  FIREBASE_PRIVATE_KEY_ID: "REPLACE_ME"
  FIREBASE_PRIVATE_KEY: "REPLACE_ME"
  FIREBASE_CLIENT_EMAIL: "REPLACE_ME"
  FIREBASE_CLIENT_ID: "REPLACE_ME"
  AWS_ACCESS_KEY_ID: "REPLACE_ME"
  AWS_SECRET_ACCESS_KEY: "REPLACE_ME"
  AWS_S3_BUCKET_NAME: "REPLACE_ME"
