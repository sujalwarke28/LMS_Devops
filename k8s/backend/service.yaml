apiVersion: v1
kind: Service
metadata:
  name: lms-backend
  namespace: lms
  labels:
    app: lms-backend
spec:
  selector:
    app: lms-backend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
